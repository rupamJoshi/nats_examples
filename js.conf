// enables jetstream, an empty block will enable and use defaults
jetstream {
    // jetstream data will be in /data/nats-server/jetstream
    store_dir: "./data"
// 1GB
    max_memory_store: 1000000000
// 10GB
    max_file_store: 1000000000
}
